server:
  port: 9001

spring:
  application:
    name: account-service

  datasource:
    url: jdbc:postgresql://localhost:5432/bank_account

  security:
    oauth2:
      client:
        registration:
          transaction-service:
            provider: keycloak
            client-id: account-service
            client-secret: 9oyxCoCbPLYOpUdVGyKYCmNY4uwgxLay
            authorization-grant-type: client_credentials
            scope: openid
        provider:
          keycloak:
            issuer-uri: http://localhost:8180/realms/bank-realm

      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/bank-realm
